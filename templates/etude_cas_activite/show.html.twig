{% extends 'base.html.twig' %}

{% block title %}EtudeCasActivite{% endblock %}

{% block body %}
    <h1>EtudeCasActivite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etude_cas_activite.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etude_cas_activite_index') }}">back to list</a>

    <a href="{{ path('app_etude_cas_activite_edit', {'id': etude_cas_activite.id}) }}">edit</a>

    {{ include('etude_cas_activite/_delete_form.html.twig') }}
{% endblock %}
