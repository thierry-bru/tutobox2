{% extends 'base.html.twig' %}

{% block title %}TypeExercice{% endblock %}

{% block body %}
    <h1>TypeExercice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_exercice.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ type_exercice.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_exercice_index') }}">back to list</a>

    <a href="{{ path('app_type_exercice_edit', {'id': type_exercice.id}) }}">edit</a>

    {{ include('type_exercice/_delete_form.html.twig') }}
{% endblock %}
