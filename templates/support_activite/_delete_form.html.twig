<form method="post" action="{{ path('app_support_activite_delete', {'id': support_activite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ support_activite.id) }}">
    <button class="btn">Delete</button>
</form>
