{% extends 'base.html.twig' %}

{% block title %}SupportActivite{% endblock %}

{% block body %}
    <h1>SupportActivite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ support_activite.id }}</td>
            </tr>
            <tr>
                <th>FileName</th>
                <td>{{ support_activite.fileName }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ support_activite.filename }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_support_activite_index') }}">back to list</a>

    <a href="{{ path('app_support_activite_edit', {'id': support_activite.id}) }}">edit</a>

    {{ include('support_activite/_delete_form.html.twig') }}
{% endblock %}
