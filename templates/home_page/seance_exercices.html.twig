 <turbo-frame id="main_page">
 <div class="row">
 <div class="col">
    <h2>{{ seance.intitule }}</h2>
 <ul class="nav nav-tabs">
    {% for exerciceHTML in seance.exercices %}
   
  <li class="nav-item">
    <a class="nav-link {% if exerciceHTML.id == currentExercice.id %}active {% endif %}" aria-current="page" href="{{ path('app_seance_exercices', {'idSeance':seance.id,'idExercice':exerciceHTML.id}) }}">{{ exerciceHTML.intitule }}</a>
  </li>

   {% endfor %}
     </ul>
     <div class="row m-0 p-0">
    {{ currentExercice.intitule}} >
    {{ currentExercice.instructions}} 
    </div>
    <div class="row">

          <iframe class="" style="height:40vh" src="  {{asset('uploads/supports/' ~ currentExercice.fichierSupport)}}" allowfullscreen="allowfullscreen"></iframe>

  </div>
  </div>
  <div class="col-3">
<h3>Progression</h3>
<div class="progress" role="progressbar" aria-label="Basic example" aria-valuenow="75" aria-valuemin="0" aria-valuemax="100">
  <div class="progress-bar" style="width: 75%"></div>
</div>
<h3>Fiches révisions</h3>
<ul>
{% for fiche in sequence.getFicheRevisions() %}
  <li><a href="{{ asset('uploads/fichesrevisions/'~fiche.getFiche()) }}">{{ fiche.titre }}</a></li>
{% endfor %}
</ul>
</div>

   </div>
   
</turbo-frame>
