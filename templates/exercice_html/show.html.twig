{% extends 'base.html.twig' %}

{% block title %}ExerciceHTML{% endblock %}

{% block body %}
    <h1>ExerciceHTML</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercice_h_t_m_l.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ exercice_h_t_m_l.title }}</td>
            </tr>
            <tr>
                <th>FileName</th>
                <td>{{ exercice_h_t_m_l.fileName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_exercice_h_t_m_l_index') }}">back to list</a>

    <a href="{{ path('app_exercice_h_t_m_l_edit', {'id': exercice_h_t_m_l.id}) }}">edit</a>

    {{ include('exercice_html/_delete_form.html.twig') }}
{% endblock %}
