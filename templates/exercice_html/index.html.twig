{% extends 'base.html.twig' %}

{% block title %}ExerciceHTML index{% endblock %}

{% block body %}
    <h1>ExerciceHTML index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>FileName</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for exercice_h_t_m_l in exercice_h_t_m_ls %}
            <tr>
                <td>{{ exercice_h_t_m_l.id }}</td>
                <td>{{ exercice_h_t_m_l.title }}</td>
                <td>{{ exercice_h_t_m_l.fileName }}</td>
                <td>
                    <a href="{{ path('app_exercice_h_t_m_l_show', {'id': exercice_h_t_m_l.id}) }}">show</a>
                    <a href="{{ path('app_exercice_h_t_m_l_edit', {'id': exercice_h_t_m_l.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_exercice_h_t_m_l_new') }}">Create new</a>
{% endblock %}
