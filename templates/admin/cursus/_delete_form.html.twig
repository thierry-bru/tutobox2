<form method="post" style="display: inline;" action="{{ path('app_cursus_delete', {'id': cursu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cursu.id) }}">
    <button class="btn btn-warning  m-2">Supprimer</button>
</form>
