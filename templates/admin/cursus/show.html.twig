 <turbo-frame id="main_page">
    <h1>Cursus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cursu.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ cursu.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cursus_index') }}"   data-turbo-frame="main_page">back to list</a>

    <a href="{{ path('app_cursus_edit', {'id': cursu.id}) }}"   data-turbo-frame="main_page">edit</a>

    {{ include('admin/cursus/_delete_form.html.twig') }}
</turbo-frame>
