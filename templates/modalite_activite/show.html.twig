{% extends 'base.html.twig' %}

{% block title %}ModaliteActivite{% endblock %}

{% block body %}
    <h1>ModaliteActivite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ modalite_activite.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ modalite_activite.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_modalite_activite_index') }}">back to list</a>

    <a href="{{ path('app_modalite_activite_edit', {'id': modalite_activite.id}) }}">edit</a>

    {{ include('modalite_activite/_delete_form.html.twig') }}
{% endblock %}
