<form method="post" action="{{ path('app_modalite_activite_delete', {'id': modalite_activite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modalite_activite.id) }}">
    <button class="btn">Delete</button>
</form>
