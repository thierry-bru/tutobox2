<form method="post" action="{{ path('app_exercice_h_p5_delete', {'id': exercice_h_p5.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercice_h_p5.id) }}">
    <button class="btn">Delete</button>
</form>
