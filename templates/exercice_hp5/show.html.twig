{% extends 'base.html.twig' %}

{% block title %}ExerciceHP5{% endblock %}

{% block body %}
    <h1>ExerciceHP5</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exercice_h_p5.id }}</td>
            </tr>
            <tr>
                <th>ContentHTML</th>
                <td>{{ exercice_h_p5.contentHTML }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_exercice_h_p5_index') }}">back to list</a>

    <a href="{{ path('app_exercice_h_p5_edit', {'id': exercice_h_p5.id}) }}">edit</a>

    {{ include('exercice_hp5/_delete_form.html.twig') }}
{% endblock %}
